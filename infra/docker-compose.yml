version: "3.8"

services:
  # ⬇️ Dein Spring-Boot Backend (GlobeNotes)
  globenotes-backend:
    build: ../GlobeNotes/backend
    container_name: globenotes-backend
    ports:
      - "8080:8080"
    volumes:
      - globenotes_uploads:/uploads
    networks:
      - app-net

  # ⬇️ HIER deine bisherigen infra-Services reinkopieren
  # z.B. Datenbank, Portainer, WordPress, whatever:
  #
  # db:
  #   image: mariadb:11
  #   environment:
  #     ...
  #   networks:
  #     - app-net
  #
  # portainer:
  #   image: portainer/portainer-ce
  #   ...

networks:
  app-net:

volumes:
  globenotes_uploads:
  # ggf. weitere Volumes von deiner bisherigen infra-compose
